name: CI

permissions:
  contents: read

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # The filesystem bridge is the riskiest part of Marginote and watchers differ by
        # platform, so it is worth proving on more than one.
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: ["22", "24"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test

  browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Install browser engines
        run: npx playwright install --with-deps chromium firefox webkit
      - name: Test Chromium, Firefox, and WebKit
        run: npm run test:e2e

  release-bundle:
    # Proves the published package actually starts, which the test suite does not.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
      - run: npm run build:release
      - run: npm run check:release
      - name: Install the packed tarball and run it
        run: |
          cd packages/cli && npm pack --silent
          mkdir -p /tmp/smoke/vault && cd /tmp/smoke
          npm init -y >/dev/null
          npm install "$GITHUB_WORKSPACE"/packages/cli/marginote-*.tgz --silent
          printf '# Smoke\n\nhello\n' > vault/smoke.md
          ./node_modules/.bin/marginote vault --port 4321 &
          sleep 5
          curl -fsS http://127.0.0.1:4321/api/files | grep -q smoke.md
          curl -fsS http://127.0.0.1:4321/ | grep -q '<title>Marginote</title>'
          echo "packaged binary serves the vault"

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Build the image
        run: docker build -t marginote:ci .
      - name: Run it against a vault
        run: |
          mkdir -p /tmp/vault && printf '# Docker\n\nhello\n' > /tmp/vault/d.md
          docker run -d --name marginote -p 4321:4321 -v /tmp/vault:/vault marginote:ci
          for i in $(seq 1 30); do curl -fsS http://127.0.0.1:4321/api/files && break || sleep 1; done
          curl -fsS http://127.0.0.1:4321/api/files | grep -q d.md
          docker logs marginote
